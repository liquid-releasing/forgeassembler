new features:

- add an inline <video> preview in the inspector's Source pane, 
- an animated mini-preview of the joiner timeline in the editor.
- Trim window editor — currently two text inputs in the Source pane; a visual scrubber with in/out handles would be vastly better.
- Multi-select bulk operations — shift-click N clips, apply same color temp / audio mode / overlay.
- Pre-forge validation — resolution mismatches, missing files, codec issues surface on Forge tab before you press Forge.
- Section / chapter naming carried into the output — each section becomes a chapter marker; show that explicitly somewhere.
- Project file I/O flow — Save / Open buttons exist in TopBar but no dialog states designed yet.
- Add additional placement options for the title card
- Add way for user to add additonal templates for the title card
- Way for user to add their own glyphs